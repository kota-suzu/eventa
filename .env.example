# データベース設定
DATABASE_USERNAME=root
DATABASE_PASSWORD=changeme
DB_HOST=db
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=api_development

# Rails環境
RAILS_ENV=development
SECRET_KEY_BASE=changeme_for_development_only

# APIサーバー
PORT=3000

# Redis
REDIS_URL=redis://redis:6379

# その他サービス
STRIPE_KEY=dummy_key

# 本番環境では以下のコマンドで安全なランダム値を生成して設定してください: 
# Rails 7/8では64バイトが推奨: `rails secret` または `openssl rand -hex 64`